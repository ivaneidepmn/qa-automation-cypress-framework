image: cypress/browsers:node18.12.0-chrome107

stages:
  - test

cypress_tests:
  stage: test
  script:
    - npm ci
    - npx cypress run
  artifacts:
    when: always
    paths:
      - cypress/screenshots
      - cypress/videos
    expire_in: 7 days